[project]
name = "operator-g1d-mujoco"
version = "0.1.0"
description = "Unitree G1-D dual-arm MuJoCo teleoperation sandbox"
requires-python = ">=3.11"
dependencies = [
  "mujoco>=3.2.0,<3.8.1",
  "numpy>=1.26",
  "pyoperator",
]

[tool.uv]
package = false

[tool.uv.sources]
pyoperator = { path = "../../../python", editable = true }
