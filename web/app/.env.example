# Public ingest URL used by the /connect QR codes. When set, the
# Connect page renders a single QR pointing at this URL instead of
# enumerating every LAN-reachable IPv4 address on the box.
#
# Leave unset to fall back to auto-detected LAN interfaces.
# OPERATOR_INGEST_URL=
