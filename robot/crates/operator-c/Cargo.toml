[package]
name = "operator-c"
version.workspace = true
edition = "2021"
description = "Stable C ABI for the Operator SDK"

[lib]
name = "operator"
crate-type = ["staticlib", "cdylib"]

[dependencies]
operator-core = { package = "operator", path = "../operator" }
